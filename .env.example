# Nebius Token Factory. Read from env or macOS Keychain; never commit the real value.
#   security add-generic-password -a "$USER" -s NEBIUS_API_KEY -w
NEBIUS_API_KEY=
NEBIUS_BASE_URL=https://api.studio.nebius.com/v1
# Frontier arm for the head-to-head benchmark and the judge.
ANTHROPIC_API_KEY=
